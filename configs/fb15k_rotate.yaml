dataset:
  name: fb15k237

model:
  name: RotatE
  hidden_channels: 128
  margin: 9.0  # RotatE uses larger margin

training:
  lr: 0.001
  epochs: 500
  batch_size: 2048
  log_every: 50

evaluation:
  batch_size: 20000
  k: 10

seed: 42
